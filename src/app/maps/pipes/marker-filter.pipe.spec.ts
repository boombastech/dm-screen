import { MarkerFilterPipePipe } from './text-filter.pipe';

describe('TextFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MarkerFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
